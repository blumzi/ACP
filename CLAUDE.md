# CLAUDE.md

## Claude's working files

- **Memory** lives in `.claude/memory/` (not in the user profile). Read `.claude/memory/MEMORY.md` at
  the start of a session, and write any new memory file there, adding its one-line pointer to that index.
- **Plans** live in `.claude/plans/`, one markdown file per plan.
